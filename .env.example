# JWT Secret Key
JWT_SECRET_KEY=your-secret-key-change-this-in-production

# PostgreSQL Database URL
# Local development:
DATABASE_URL=postgresql://localhost/ireporter
# Render (copy the Internal Database URL from your Render PostgreSQL dashboard):
# DATABASE_URL=postgresql://user:password@host/dbname
